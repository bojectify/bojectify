# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
pnpm-lock.yaml

.nx/self-healing

# Bundled template — formatting is managed inside the template itself
plugins/platform/template/

# Skill reference blocks — placeholder templates, not formatted
plugins/platform/skills/scaffold-dev-container/blocks/