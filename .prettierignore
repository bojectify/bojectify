# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
pnpm-lock.yaml

.nx/self-healing

# Bundled template — formatting is managed inside the template itself
plugins/platform/template/

# Compose block fragments — intentionally indented for merging
plugins/platform/skills/scaffold-dev-container/blocks/compose/