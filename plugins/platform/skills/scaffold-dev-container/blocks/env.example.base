# Service URLs. The dev container also sets these via compose `environment`
# (which overrides env_file); these values are the host-lane defaults / docs.
# Uncomment the ones matching the sidecars you enabled:
# REDIS_URL=redis://redis:6379
# DATABASE_URL=postgresql://boject:boject@db:5432/boject
# MEILI_URL=http://meilisearch:7700
